import 'package:flutter/rendering.dart';

class ViewMeasurements {
  ViewMeasurements._();

  static const BORDER_RADIUS_TOP =
      BorderRadius.vertical(top: Radius.circular(30.0));

  static const BORDER_RADIUS_RIGHT =
      BorderRadius.horizontal(right: Radius.circular(20.0));

  static const BORDER_RADIUS_LEFT =
      BorderRadius.horizontal(left: Radius.circular(20.0));

  static const CIRCULAR_AV_RADIUS = 40.0;
}
